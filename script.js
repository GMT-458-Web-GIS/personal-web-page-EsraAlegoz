function setLanguage(language) {
    if (language === 'en') {
        document.body.lang = 'en';
        alert('Language set to English');
    } else if (language === 'tr') {
        document.body.lang = 'tr';
        alert('Dil Türkçe olarak ayarlandı');
    }
}
